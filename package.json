{
  "name": "n8n-readonly-mcp",
  "version": "1.1.0",
  "description": "Hardened read-only MCP proxy for n8n with secret scrubbing, rate limiting, and input validation",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js",
    "proxy": "node --env-file=.env proxy.js",
    "start:production": "node index.js",
    "proxy:production": "node proxy.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.12.1",
    "zod": "3.23.8"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
